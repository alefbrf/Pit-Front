export default interface IProduct {
    name: string,
    id: number,
    description: string,
    price: number,
    imageUrl: string,
    favorite: boolean,
    checked: boolean,
    quantity: number
}